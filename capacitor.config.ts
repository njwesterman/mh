import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.esaf.mh',
  appName: 'mh',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
